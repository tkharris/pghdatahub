.row.ds_item
	.span5
		%h2= item['doc_title']
		%h3
			via
			%u= item['doc_dept']
		.row
			.span5
				Juristiction:
				= item['juris']
		.row
			.span5
				406
				downloads
		.row
			.span5
				Created:
				= item['created_date'] || 'Unknown'
		- #Data columns

	.span2
		%h2 Download:
		-# for DataSet.types.each do |t|
			-# @dl = @item.send(t)
			-# if @dl
				- #link_to(image_tag(t + '.png'), dl)
		.row
			.span2
				%a.dl_link{:href => item['doc_reference']}
					%i{:class => "icon-download", :style=>'font-size: 4em'}
